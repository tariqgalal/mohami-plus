# قرارات محتاجة من المالك

> الحاجات اللي مش صلّحناها بأنفسنا لأنها بره نطاق المرحلة الحالية أو محتاجة قرار تجاري/تكلفة.

---

## القرار 1: تخزين المستندات على Vercel (urgent قبل الإطلاق)

### السياق
Vercel Functions filesystem **read-only في الإنتاج** (`/tmp` فقط ولكن مش persistent بين requests). الكود حالياً عند موديول الـ documents/upload بيستخدم مسارات local (مثل `/uploads/...`). على المحلي ده شغّال، لكن أول مكتب يرفع مستند على الإنتاج هيقع.

### الخيارات
| # | الخيار | المميزات | العيوب | التكلفة الشهرية تقريبية |
|---|---|---|---|---|
| 1 | **Supabase Storage** (لو DB أصلاً عليه) | تكامل مباشر مع DB، RLS موجود، رخيص | لازم تعديل كود الرفع | مجاني حتى 1GB، بعدها $0.021/GB |
| 2 | **Vercel Blob** | تكامل native، DX جيد | أغلى من Supabase | $0.15/GB/شهر |
| 3 | **Cloudflare R2** | الأرخص لما تكبر، صفر egress | لازم setup منفصل | $0.015/GB + صفر egress |

### توصيتي
**Supabase Storage** — قاعدة البيانات أصلاً عندكم على Supabase، فالـ stack ما يتشعّبش، والمكتب يقدر يستخدم RLS عشان يضمن إن مستندات مكتب ما يقدرش يقراها مكتب تاني.

### التأثير لو ما اتعملش
أول رفع مستند من أي مكتب على الإنتاج هيرجع 500. فيها كسر تجربة. **لازم قبل ما تفتح للبيع.**

### وقت التنفيذ التقريبي
2-3 ساعات (تعديل `/api/upload`, helper `lib/storage.ts`, migration للملفات الموجودة لو فيه).

---

## القرار 2: شغّل الـ E2E suite تلقائياً مع كل deploy؟

### السياق
الـ suite الحالي بياخد دقيقتين على الإنتاج وبيكتشف أي regression في tenant isolation. كل ما الـ codebase يكبر، فرصة كسر العزل بتزيد. لو الـ suite ما اشتغلش تلقائياً، الكشف هيكون يدوي وغالباً متأخر.

### الخيارات
1. **GitHub Action** بعد كل push على `main`: يعمل deploy، يستنّى يجهز، ويشغّل `TEST_ENV=production npm run test:e2e`. لو فشل، يعمل rollback أو ينبّه.
2. **Vercel Deploy Hook + external runner** على نفس المنطق.
3. **Manual فقط** — يدوي بعد كل تغيير حساس.

### توصيتي
الخيار 1 — أضمن طريقة لاكتشاف regressions أمنية بدري.

### التأثير لو ما اتعملش
ثغرة tenant isolation ممكن تعدّي لشهور قبل ما المالك يكتشفها — ولما تكتشف، البيانات تكون اتسرّبت بالفعل.

### وقت التنفيذ التقريبي
ساعة (إضافة `.github/workflows/e2e.yml`، إنشاء `PLAYWRIGHT_BASE_URL` secret، اختبار).

---

## ملاحظات إضافية مش محتاجة قرار حالاً

- **اختبار حدود الباقات** يحتاج seed بيانات ضخمة (25 قضية، 3 مستخدمين كاملين، إلخ). أنشئ task بـ priority متوسطة لما تيجي تجهّز للإطلاق.
- **اختبارات الواجهة على Tablet/Mobile** يدوياً قبل الإطلاق — مش مغطّاة في الـ suite الحالي.
- **Security headers checker** على securityheaders.com: عند الفحص يدوياً، تأكدوا إن النتيجة A أو أعلى. لو أقل، ضِفوا `next.config.ts > headers()` لـ `Content-Security-Policy`, `X-Frame-Options`, `Referrer-Policy`.
