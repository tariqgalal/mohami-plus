# تقرير إصلاحات ملاحظات العميل — سبتمبر 2026

**التاريخ:** 2026-09-03
**الحالة:** 9/9 مهام مكتملة ✅ — `npm run build` ناجح

---

## ملخص سريع

| # | المهمة | الحالة | الملاحظة |
|---|--------|--------|----------|
| 1 | تبويبات حالات القضايا | ✅ | السبب الجذري كان في **نموذج الإنشاء** لا في التبويبات |
| 2 | فلتر نوع القضية | ✅ | الفلتر كان سليماً — المشكلة نفس سبب المهمة 1 |
| 3 | تبويبات صفحة الأحكام | ✅ | حُذف "ما قبل قيد الدعوى" + أُضيف "مكتسب القطعية" |
| 4 | تسمية "رقم المعاملة" | ✅ | |
| 5 | تاريخ رفع الدعوى | ✅ | كان يفشل بصمت عند تركه فارغاً |
| 6 | التاريخ الهجري/الميلادي | ✅ | يسري على **كل** نماذج المشروع |
| 7 | حفظ الوكالة | ✅ | السبب: رسائل الخطأ كانت تُبتلع |
| 8 | رفع الملفات والصور | ✅ | **الـ bucket لم يكن موجوداً — أُنشئ الآن** |
| 9 | حقل "الفرع" | ✅ | كان يعمل — تم التحقق والتحسين |

---

## المهمة 1 — تبويبات حالات القضايا

### ما وُجد فعلاً

ثلاث مشاكل منفصلة، أهمها لم تكن في التبويبات أصلاً:

**أ) تبويب "معلقة" مكرر** — `CASE_STATUS` كان فيه مفتاحان بنفس التسمية حرفياً:
```
ON_HOLD:   "معلقة"
SUSPENDED: "معلقة"   ← تكرار
```
وكلاهما مُدرج في `PRIMARY_ORDER`، فظهر التبويب مرتين. أُبقي `ON_HOLD` وأُخرج `SUSPENDED`
من القائمة المعروضة، مع حارس برمجي في المكوّن يُسقط أي تسمية مكررة لو رجعت مستقبلاً.

**ب) "مسربة"** — لا يوجد هذا النص في المشروع إطلاقاً (بحثت في كل الملفات).
الذي كان معروضاً هو **"مسودة"** (`DRAFT`) — وهي فعلاً ليست حالة قضية في السياق
القضائي السعودي، فأُخرجت من القائمة.

`DRAFT` و `SUSPENDED` بقيتا في enum قاعدة البيانات وفي `CASE_STATUS_LEGACY` حتى
تُعرض بالعربي لو وُجد سجل قديم محفوظ بهما — لكنهما لم تعودا خياراً للاختيار.

**ج) السبب الجذري لظهور "لا توجد نتائج" — وهو الأهم** 🔴

حقل **حالة القضية كان يظهر في وضع التعديل فقط**:
```tsx
{mode === "edit" && (            ← عند الإنشاء لا يوجد حقل حالة إطلاقاً
  <Select id="status" ...>
)}
```
فكل قضية جديدة تُحفظ بـ `OPEN` إجبارياً. تحقّقتُ من قاعدة البيانات:
**104 قضية، كلها بلا استثناء `OPEN`** — ومكتب "شيد القانون" قضاياه الأربع كلها `OPEN`.

فالتبويبات كانت تعمل بشكل صحيح تماماً؛ ببساطة **لم تكن هناك أي قضية بحالة أخرى
يمكن أن تظهر**. أصبح حقل الحالة متاحاً عند الإنشاء أيضاً.

### التبويبات بعد الإصلاح
`الكل` · `مفتوحة` · `جارية` · `منظورة` · `ما قبل قيد الدعوى` · `معلقة` · `مستأنفة` · `مكسوبة` · `خاسرة` · `تسوية` · `مغلقة`

---

## المهمة 2 — فلتر نوع القضية

### ما وُجد

**منطق الفلترة سليم ولم يكن به عطل.** اختبرته مباشرة على بيانات مكتب العميل:

```
counts: {"OPEN": 4}, total: 4
{}                        => 4 قضايا
{caseType: "EXECUTION"}   => 0     ← صحيح
{caseType: "COMMERCIAL"}  => 2
{status: "PENDING"}       => 0     ← صحيح
{status: "OPEN"}          => 4
```

قضايا المكتب الأربع أنواعها `COMMERCIAL` و `LABOR` — **لا توجد فيها قضية نوعها
"تنفيذ" أصلاً**، فالنتيجة الفارغة كانت صحيحة. الـ dropdown يرسل مفتاح الـ enum
(`EXECUTION`) والـ API يقارن به مباشرة — لا يوجد أي تعارض بين العربي والإنجليزي.

### ما تغيّر رغم ذلك

رسالة "لا توجد نتائج" المجرّدة كانت تبدو وكأنها عطل. أصبحت تسرد الفلاتر الفعّالة
ومعها زر مسح مباشر:

> **لا توجد قضايا مطابقة**
> لا توجد قضايا تطابق (النوع: تنفيذ، الأولوية: عالية). امسح عوامل التصفية لعرض كل القضايا.
> `[ مسح عوامل التصفية ]`

### التحقق النهائي (بعد إصلاح المهمة 1)

أنشأتُ قضية اختبارية نوعها "تنفيذ" وحالتها "منظورة" وحذفتها بعد التحقق:
```
persisted: status=PENDING, caseType=EXECUTION, branch="فرع الرياض", filingDate=2026-07-31

filter {status: PENDING}                   => 1 ✓
filter {caseType: EXECUTION}               => 2 ✓
filter {status: PENDING, caseType: EXECUTION} => 1 ✓   (الفلاتر المركّبة تعمل)
```

---

## المهمة 3 — تبويبات صفحة الأحكام

`PRE_FILING` ("ما قبل قيد الدعوى") حُذف من تبويبات الأحكام وخيارات النموذج — هو حالة
قضية وليس حالة اعتراض على حكم. القيمة بقيت في الـ enum وفي `OBJECTION_STATUS_LEGACY`
للعرض فقط.

أُضيفت حالة **"مكتسب القطعية"** (`FINAL`) إلى `ObjectionStatus` عبر migration:
`prisma/migrations/20260903000000_judgment_final_objection_status` — **مُطبَّقة على
قاعدة بيانات الإنتاج بالفعل** ✅

**التبويبات النهائية:** `الكل` · `بدون اعتراض` · `قيد المهلة` · `تم الاعتراض` · `مكتسب القطعية`

> ملاحظة: لا توجد أي أحكام مسجّلة في قاعدة البيانات حالياً (0 سجل)، فالتبويبات كلها
> ستظهر بعداد صفر حتى يُسجَّل أول حكم.

---

## المهمة 4 — تسمية "رقم المعاملة"

| الموضع | قبل | بعد |
|--------|-----|-----|
| Label في النموذج | رقم المعاملة في المنشأة | **رقم المعاملة** |
| Placeholder | رقم مرجعي داخلي | **رقم المعاملة** |
| صفحة تفاصيل القضية | رقم المعاملة في المنشأة | **رقم المعاملة** |

اسم العمود في قاعدة البيانات (`establishmentTxnNumber`) لم يُمس.

---

## المهمة 5 — تاريخ رفع الدعوى

### السبب الجذري

الحقل معرّف بـ `z.coerce.date()`. المتصفح يرسل `""` لحقل تاريخ فارغ، و:

```
z.coerce.date()  ⟵  ""  =  new Date("")  =  Invalid Date  →  فشل التحقق
```

ولأن النموذج **لم يكن يعرض أي رسالة خطأ لهذا الحقل**، كان الضغط على "التالي" في
الويزارد لا يفعل شيئاً إطلاقاً — لا انتقال ولا رسالة. يبدو للمستخدم تماماً وكأن
التاريخ "لا يُحفظ".

### الإصلاح

`optionalDate` جديد في `src/lib/validations/case.ts`:
- `""` أو `null` ⇒ `null` (لا خطأ)
- `"YYYY-MM-DD"` ⇒ تاريخ عند **منتصف نهار UTC** — يمنع انزياح اليوم باختلاف المنطقة الزمنية
- تاريخ غير صالح ⇒ خطأ **يظهر الآن تحت الحقل**

أُصلح معه خطآن مجاوران بنفس السبب:
- **قيمة القضية:** `""` كانت تُحوَّل إلى `0` وتُحفظ صفراً — أصبحت `null`
- **النصوص الاختيارية:** `""` كانت تُحفظ كسلسلة فارغة — أصبحت `null`
  (كان حقل "الفرع" محفوظاً `''` في قاعدة البيانات فعلاً)

**التحقق:**
```
{filingDate: "",           value: ""}      => OK  filing=null  value=null
{filingDate: "2026-07-31", value: "1500.5"}=> OK  filing=2026-07-31T12:00:00Z  value=1500.5
{filingDate: "not-a-date"}                 => FAIL برسالة ظاهرة للمستخدم
```

> صفحة تعديل القضية كانت سليمة أصلاً (تقصّ الـ ISO إلى `YYYY-MM-DD`).

---

## المهمة 6 — التاريخ الهجري والميلادي

أُضيف مبدّل **`هجري | ميلادي`** داخل المكوّن المشترك `HijriDatePicker`:

- **هجري:** القوائم الثلاث (سنة/شهر/يوم) بتقويم أم القرى — كما كان تماماً
- **ميلادي:** حقل تاريخ عادي بتقويم المتصفح

في الحالتين تُحفظ القيمة في قاعدة البيانات **ميلادية** (`YYYY-MM-DD`) ويُحسب المقابل
الهجري ويُخزَّن معها. التحويل يتم عند منتصف نهار UTC فلا يحدث انزياح يوم. التبديل بين
التقويمين لا يفقد التاريخ المختار، والسطر أسفل الحقل يعرض المقابل في التقويم الآخر.

**التحقق (ذهاباً وإياباً):**
```
2026-09-03 => هجري 1448-03-21 => ميلادي 2026-09-03 ✓
2026-01-01 => هجري 1447-07-12 => ميلادي 2026-01-01 ✓
2025-12-31 => هجري 1447-07-11 => ميلادي 2025-12-31 ✓
2027-06-15 => هجري 1449-01-10 => ميلادي 2027-06-15 ✓
```

### لماذا هذا يغطي كل النماذج

التعديل في المكوّن المشترك وحده، وكل النماذج تستهلكه بنفس الواجهة
(`{gregorian, hijri}`)، فالمبدّل ظهر تلقائياً في:

**المهام** (إنشاء + تعديل) · **الاستشارات** · الوكالات · الأحكام · المعاملات ·
الاستعارات · إجازات الموظفين · تقارير الإجازات

أُزيلت كلمة "(هجري)" من تسميات هذه الحقول لأنها صارت مضلِّلة.

---

## المهمة 7 — حفظ الوكالة

### السبب الجذري

`handleApiError` كان يرد **"حدث خطأ غير متوقع" (500)** على أي `Error` عادي. والخدمات
كانت ترمي أخطاءً برسائل عربية صحيحة تماماً لكن بـ `new Error()` العادي:

```ts
if (!client) throw new Error("العميل غير موجود");   // ← ابتُلعت الرسالة
```

**أعدتُ إنتاج الخطأ حرفياً** على الـ API المحلي:
```
POST /api/powers-of-attorney  (بعميل لا يتبع نفس المكتب)
قبل:  {"error": "حدث خطأ غير متوقع"}   [HTTP 500]
بعد:  {"error": "العميل غير موجود"}     [HTTP 404]
```

### الإصلاح

- ملف جديد `src/lib/errors.ts`: `AppError` / `NotFoundError` / `ServiceUnavailableError`
  مع كود HTTP ورسالة عربية، و`handleApiError` يمرّرها كما هي
- حُوِّلت **30 رمية `Error` عادية في 14 خدمة** إلى الأصناف الجديدة
  ("غير موجود" ⇒ 404، الباقي ⇒ 400)
- `apiErrorMessage`: يضم أخطاء الحقول لرسالة الـ toast — بدل "بيانات غير صحيحة"
  المبهمة يظهر: *"بيانات غير صحيحة: تاريخ البداية الهجري مطلوب، يجب اختيار موظف واحد على الأقل"*
  (طُبِّق في 20 hook)
- **نموذج الوكالة:** ملخص الأخطاء كان يقول "راجع الحقول المظللة بالأحمر" — وهي نصيحة
  عديمة الفائدة لأن حقلي `startDateHijri` / `endDateHijri` يُملآن ضمنياً من مُدخل
  التاريخ **ولم يكن لهما أي تظليل أو رسالة**. أصبح الملخص يسرد الرسائل الفعلية،
  وأخطاء التاريخ الهجري تظهر تحت مُدخل التاريخ

**التحقق:** إنشاء وكالة كاملة عبر الـ API نجح ثم حُذفت — `HTTP 201` ✅

> ملاحظة: كان في قاعدة البيانات **0 وكالة** قبل الإصلاح، وهو ما يؤكد أن الحفظ لم
> ينجح ولا مرة. أرجّح أن رسالة "حدث خطأ غير متوقع" التي رآها العميل جاءت من **رفع
> المرفق داخل نموذج الوكالة** (المهمة 8) — نفس الرسالة بالضبط ونفس السبب البرمجي.
> على كل حال، أي فشل مستقبلي سيظهر الآن بسببه الحقيقي بدل الرسالة المبهمة.

---

## المهمة 8 — رفع الملفات والصور

### السبب الجذري — مؤكَّد 🔴

استعلمتُ قاعدة بيانات Supabase مباشرة:

```sql
select id, name from storage.buckets;
-- []   ← لا يوجد ولا bucket واحد في المشروع بأكمله
select count(*) from storage.objects;
-- 0
```

**الـ bucket `mohami-files` لم يكن موجوداً إطلاقاً.** الكود كان سليماً تماماً —
`src/lib/storage.ts` يستخدم Supabase Storage بشكل صحيح (لا `fs.writeFile`)، ومتغيرات
البيئة الثلاثة موجودة في Vercel، والـ API route يتعامل مع `multipart/form-data` ويحفظ
الـ metadata. لكن كل عملية رفع كانت ترتد بـ `Bucket not found`، وهذا الخطأ يُرمى بـ
`new Error()` عادي فيتحوّل إلى **"حدث خطأ غير متوقع"** — وهذا هو ما رآه العميل.

### ما نُفِّذ

**1. أُنشئ الـ bucket** — شُغِّل `supabase/storage-setup.sql` الموجود في المستودع على
مشروع Supabase الإنتاجي (بموافقتك)، وتم التحقق:

```
BUCKETS: [{ id: "mohami-files", public: false, file_size_limit: 10485760, mimes: 10 }]
POLICIES: mohami_files_block_anon_read (SELECT)
          mohami_files_block_anon_write (INSERT)
          mohami_files_block_anon_delete (DELETE)
```
bucket خاص (غير عام) · حد 10 ميجابايت · 10 أنواع MIME مسموحة · وصول anon/authenticated
المباشر محظور (التطبيق فقط عبر `service_role` وروابط موقّعة مؤقتة).

**2. رسائل خطأ حقيقية** — أخطاء التخزين أصبحت `ServiceUnavailableError` (503) برسائل
عربية محددة، مع الرسالة الأصلية من المزوّد في `details` للتشخيص:

| حالة المزوّد | الرسالة للمستخدم |
|---|---|
| `Bucket not found` | خدمة تخزين الملفات غير مهيّأة (مساحة التخزين "mohami-files" غير موجودة). تواصل مع مسؤول النظام. |
| تجاوز الحجم | حجم الملف يتجاوز الحد المسموح به. |
| MIME غير مدعوم | نوع الملف غير مدعوم. |
| متغيرات البيئة ناقصة | خدمة تخزين الملفات غير مهيّأة. تواصل مع مسؤول النظام. |

**التحقق** (بمفاتيح خاطئة عمداً لإجبار الفشل):
```
قبل:  {"error": "حدث خطأ غير متوقع"}                          [HTTP 500]
بعد:  {"error": "فشل رفع الملف. حاول مرة أخرى أو تواصل مع مسؤول النظام.",
       "details": "Invalid Compact JWS"}                       [HTTP 503]
```

**3. `FileUpload`** — فحص الحجم والامتداد **قبل** الرفع (بدل انتظار فشل من السيرفر)،
وعرض رسالة السيرفر الحقيقية.

### ⚠️ مطلوب منك: تأكيد واحد

الـ bucket أُنشئ والكود جاهز، لكنني **لا أملك `SUPABASE_SERVICE_ROLE_KEY`** (Vercel
يخفي قيم المتغيّرات الحسّاسة عند السحب)، فلم أستطع تنفيذ عملية رفع فعلية على الإنتاج.

**بعد النشر، ارفع ملف PDF أو صورة واحدة من أي شاشة** (الوكالات أو القضايا أو المستندات)
للتأكد من نجاح المسار كاملاً. لو ظهر خطأ فسيكون الآن **برسالة واضحة تحدد السبب**.

---

## المهمة 9 — حقل "الفرع"

الحقل كان يعمل بالفعل: `branch String?` موجود في `Case` بالـ schema وفي قاعدة بيانات
الإنتاج، والنموذج يرسله والـ API يحفظه، ويظهر في صفحة تفاصيل القضية.

**ما تحسّن:** كان يُحفظ سلسلة فارغة `''` عند تركه فارغاً (تحقّقت من ذلك في بيانات
الإنتاج) — أصبح يُحفظ `null` بشكل صحيح.

**التحقق:** `branch: "فرع الرياض"` حُفظ وقُرئ بنجاح ✅

---

## الـ Commits

```
d6fe1ab  fix(errors):    رسائل خطأ حقيقية بدل "حدث خطأ غير متوقع"        (7، 8)
34ab55c  feat(dates):    إتاحة اختيار التاريخ بالهجري أو الميلادي          (6)
32c05e6  fix(judgments): تصحيح تبويبات حالة الاعتراض                      (3)
14d99ca  fix(cases):     status tabs, filters, date picker & field labels  (1، 2، 4، 5، 9)
```

---

## ملاحظات إضافية اكتُشفت أثناء العمل

1. **نمط خطأ متكرر في كل المشروع** — 30 موضعاً في 14 خدمة كانت رسائلها العربية
   الصحيحة تُبتلع وتتحول إلى "حدث خطأ غير متوقع": الفواتير، الجلسات، المستندات،
   المراسلات، الفريق، المهام، وغيرها. كلها أُصلحت، لا الوكالات وحدها.

2. **المكتب التجريبي بلغ حد الباقة** — `demo-firm` عنده 100 قضية = الحد الأقصى،
   فمحاولة إضافة قضية جديدة ترد *"وصلت للحد الأقصى للقضايا (100). يرجى ترقية الباقة."*
   سلوك صحيح، لكن انتبه له عند الاختبار على هذا المكتب.

3. **قواعد المشروع مُحترمة** — كل استعلام يمر بـ `tenantId`؛ لم تُمس إعدادات
   NextAuth ولا أسعار الباقات (199/499/999)؛ كل النصوص الجديدة عربية RTL.

4. **الترحيلات** — المشروع يستخدم `prisma migrate deploy` في `npm run build` (وليس
   `db push`)، لذلك أنشأتُ ملف migration نظامياً للـ enum الجديد بدل `db push` حتى
   لا يحدث drift عند البناء على Vercel. الترحيل مُطبَّق على الإنتاج بالفعل.
