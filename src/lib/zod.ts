import { z } from "zod";

// تفعيل اللغة العربية كلغة افتراضية لرسائل التحقق في Zod (v4 locales).
// بهذا تظهر رسائل مثل "مطلوب" و"بريد إلكتروني غير مقبول" بالعربي في كل
// النماذج (عبر zodResolver) وفي التحقق على السيرفر، دون الحاجة لكتابة رسالة
// يدوية لكل حقل. لكتابة رسالة مخصصة لحقل معيّن مرّر message: كالمعتاد.
z.config(z.locales.ar());

export { z };
